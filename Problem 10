##The sum of the primes below 10 is 2 + 3 + 5 + 7 = 17.##
##Find the sum of all the primes below two million.##

num <- 2:2000000

prime <- c()

while(length(num)>=1){
   p <- num[1]
   a <- which(num%%p == 0)
   num <- num[-a]
   prime <- c(prime,p)
}

sum(as.numeric(prime))
